sudo: required
language: cpp
compiler:
  - gcc
os:
  - linux
before_script:
  - chmod u+x *
script:
  - export GCC_REPO=$(pwd)
  - cd ..
  - echo Current directory
  - echo $(pwd)
  - exit 1
branches:
  only:
    - master
